apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Validate=false
    openshift.io/host.generated: "true"
  name: awpks
spec:
  tls:
    termination: edge
  to:
    kind: Service
    name: andy-wang-pipeline-from-scratch # is this the namespace?
    weight: 100
  wildcardPolicy: None
